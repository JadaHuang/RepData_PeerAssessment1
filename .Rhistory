reg
barplot(reg, ccol = "wheat", main = "Number of Counties in Each Region")
barplot(reg, col = "wheat", main = "Number of Counties in Each Region")
boxplot(pollution$pm25 ~ pollution$region)
boxplot(pm25 ~ region, data = pollution, col = "red")
par(mfrow = c(2, 1), mar = c(4, 4, 2, 1))
east <- subset(pollution, pollution$region=="east")
east <- subset(pollution, region=="east")
head(east)
hist(east$pm25, col = "green")
east <- subset(pollution, region=="weat")
hist(subset(pollution, region=="weat")$pm25, col = "green")
hist(subset(pollution, region=="west")$pm25, col = "green")
with(pollution) plot(latitude, pm25)
with(pollution)
with(pollution, plot(latitude, pm25))
abline(h = 12, lwd = 2, lty = 2)
plot(pollution$latitude, ppm, col = pollution$region)
abline(h = 12, lwd = 2, lty = 2)
par(mfrow = c(1, 2), mar = c(5, 4, 2, 1))
west <- subset(pollution, region=="west")
plot(west$latitude, west$pm25, main = "west")
plot(west$latitude, west$pm25, main = "west")
plot(west$latitude, west$pm25, main = "West")
plot(east$latitude, east$pm25, main = "east")
plot(east$latitude, east$pm25, main = "East")
q()
library(swirl)
swirl()
?Devices
with(faithful, plot)
with(faithful, plot(eruptions, waiting))
title(main = "Old Faithful Geyser data")
dev.cur()
pdf(file = "myplot.pdf")
with(faithful, plot(eruptions, waiting))
title(main = "Old Faithful Geyser data")
dev.cur()
dev.off()
dev.cur()
with(faithful, plot(eruptions, waiting))
title(main = "Old Faithful Geyser data")
dev.copy(png, file = "geyserplot.png")
dev.off()
q
q()
x<- read.csv("車次A整理資料.csv",header=T,sep=",")
TicRecord  <- subset(x,,select= c(起站,訖站,TRNS_DatE,BOARD_DATE, 張數))
#更改票務資料時間格式
View(TicRecord)
a<-as.Date(strptime(TicRecord[k,3],format='%Y%m%d',))
a<-as.Date(strptime(TicRecord[1,3],format='%Y%m%d',))
class(a)
TicRecord[1,5]<-a
x<- read.csv("車次A整理資料.csv",header=T,sep=",")
TicRecord  <- subset(x,,select= c(起站,訖站,TRNS_DatE,BOARD_DATE, 張數))
#更改票務資料時間格式
a<-as.Date(strptime(TicRecord[1,3],format='%Y%m%d',))
class(a)
TicRecord[1,3]<-a
x<- read.csv("車次A整理資料.csv",header=T,sep=",")
TicRecord  <- subset(x,,select= c(起站,訖站,TRNS_DatE,BOARD_DATE, 張數))
#更改票務資料時間格式
a<-as.Date(strptime(TicRecord[,3],format='%Y%m%d',))
TicRecord <- cbind(TicRecord, a)
q()
install.packages(c("boot", "chron", "class", "cluster", "codetools", "crayon", "dplyr", "foreign", "httpuv", "httr", "jsonlite", "KernSmooth", "lattice", "MASS", "Matrix", "mgcv", "mime", "nlme", "nnet", "plyr", "quantmod", "R6", "Rcpp", "RCurl", "rJava", "RMySQL", "rpart", "scales", "spatial", "stringi", "survival", "tidyr", "TTR", "XML"))
setwd("C:\Users\2033\Documents\LearnR\Reproducible Research\RepData_PeerAssessment1")
getwd()
setwd("C:/Users/2033/Documents/LearnR/Reproducible Research/RepData_PeerAssessment1")
dir()
read.csv("activity.zip")
read.csv("activity.zip")
zip.unpack("activity.zip")
f <- unzip("activity.zip")
f
unzip("activity.zip")
mydata <- read.csv("activity.csv")
mydata <- read.csv("activity.csv")
View(mydata)
class(mydata)
View(mydata)
tapply(mydata$steps, mydata$date, sum)
?tapply
?sum
tapply(mydata$steps, mydata$date, sum, na.rm = TRUE)
plot(tapply(mydata$steps, mydata$date, sum, na.rm = TRUE))
hist(tapply(mydata$steps, mydata$date, sum, na.rm = TRUE))
total_steps_per_day <- tapply(mydata$steps, mydata$date, sum, na.rm = TRUE)
total_steps_per_day
hist(total_steps_per_day)
mean(total_steps_per_day)
median(total_steps_per_day)
df <- read.csv("activity.csv")
df$date <- as.Date(df$date)
df <- read.csv("activity.csv")
df$date <- as.Date(df$date)
summery(df)
summary(df)
total_steps_per_day <- tapply(mydata$steps, mydata$date, sum, na.rm = TRUE)
total_steps_per_day <- tapply(df$steps, df$date, sum, na.rm = TRUE)
total_steps_per_day <- tapply(df$steps, df$date, sum, na.rm = TRUE)
total_steps_per_day <- tapply(df$steps, df$date, sum, na.rm = TRUE)
total_steps_per_day
?plot
plot(df$interval, df$steps)
plot(df$interval, mean(df$steps))
plot(df$interval, tapply(df$steps, df$interval, mean))
tapply(df$steps, df$interval, mean)
plot(df$interval, tapply(df$steps, df$interval, mean, na.rm=T))
tapply(df$steps, df$interval, mean, na.rm=T)
tapply(df$steps, df$interval, mean, na.rm=T)
plot(df$interval, tapply(df$steps, df$interval, mean, na.rm=T))
plot(tapply(df$steps, df$interval, mean, na.rm=T))
plot(tapply(df$steps, df$interval, mean, na.rm=T), type=l)
plot(tapply(df$steps, df$interval, mean, na.rm=T), type=1)
plot(tapply(df$steps, df$interval, mean, na.rm=T), type="l")
max(tapply(df$steps, df$interval, mean, na.rm=T))
which.max(tapply(df$steps, df$interval, mean, na.rm=T))
tapply(df$steps, df$interval, mean, na.rm=T)
average_steps_by_interval <- tapply(df$steps, df$interval, mean, na.rm=T)
average_steps_by_interval <- tapply(df$steps, df$interval, mean, na.rm=T)
average_steps_by_interval
plot(average_steps_by_interval, names(average_steps_by_interval), type="l")
plot(average_steps_by_interval, type="l")
plot(names(average_steps_by_interval), average_steps_by_interval, type="l")
which.max(average_steps_by_interval)
names(which.max(average_steps_by_interval))
length(df)
raw(df)
nraw(df)
nrow(df)
nrow(df[df$steps = NA])
nrow(df[df$steps == NA])
nrow(df[df$steps is NA])
nrow(df[df$steps = "NA"])
nrow(df[,df$steps = "NA"])
nrow(df[,df$steps = 0])
nrow(df[,df$steps == 0])
nrow(df[df$steps=0,])
df$steps[=NA]
df[df$steps=NA,]
df[df$steps==NA,]
nrow(df[df$steps==NA,])
View(df)
nrow(df[df$steps==NA,])
df <- read.csv("activity.csv")
nrow(df[df$steps==NA,])
nrow(df)
View(df)
df[df$steps==NA,]
summary(df$steps)
is.na(df$steps)
nrow(df[is.na(df$steps)])
nrow(df[is.na(df$steps),])
df[is.na(df$steps),]
new_df <- merge(df, average_steps_by_interval, by = "interval")
new_df <- merge(x = df, y = average_steps_by_interval, by = "interval")
average_steps_by_interval
names(average_steps_by_interval)
names(average_steps_by_interval)
class(average_steps_by_interval)
as.table(average_steps_by_interval)
names(average_steps_by_interval)
average_steps_by_interval <- as.table(average_steps_by_interval)
names(average_steps_by_interval)
class(average_steps_by_interval)
df <- read.csv("activity.csv")
df$date <- as.Date(df$date)
total_steps_per_day <- tapply(df$steps, df$date, sum, na.rm = TRUE)
hist(total_steps_per_day)
average_steps_by_interval <- tapply(df$steps, df$interval, mean, na.rm=T)
nrow(df[is.na(df$steps),])
new_df <- merge(df, average_steps_by_interval)
View(new_df)
?merge
colnames(average_steps_by_interval)
row.names(average_steps_by_interval)
colnames(average_steps_by_interval)
colnames
new_df <- merge(df, average_steps_by_interval, by.x = "interval")
new_df <- merge(df, average_steps_by_interval, by.x = "interval", by.y = names(average_steps_by_interval))
dim(average_steps_by_interval)
x <- as.list(average_steps_by_interval)
x
average_steps_by_interval[1]
average_steps_by_interval[2]
average_steps_by_interval["5"]
new_df <- merge(df, average_steps_by_interval, by.x = "interval", by.y = row.names(average_steps_by_interval))
x
x <- as.data.frame(average_steps_by_interval)
x
names(x)
names(x) <- c("interval", "steps")
new_df <- merge(df, average_steps_by_interval, by.x = "interval"
)
row.names(x)
x <- cbind(row.names(x), x)
x
names(x)
average_steps_by_interval
names(x)
names(x) <- c("interval", "steps")
x
names(x)
new_df <- merge(df, x, by = "interval", all.x = T)
View(new_df)
new_df[is.na(new_)]
new_df[is.na(new_df$steps.x),]
new_df[is.na(new_df$steps.x),"steps.x"]
new_df[is.na(new_df$steps.x),"steps.x"] <- new_df[is.na(new_df$steps.x),"steps.y"]
new_df
new_total_steps_per_day <- tapply(new_df$steps.x, new_df$date, sum, na.rm = TRUE)
hist(new_total_steps_per_day)
mean(new_total_steps_per_day)
median(new_total_steps_per_day)
nrow(df[is.na(df$steps),])
x <- as.data.frame(average_steps_by_interval)
x <- cbind(row.names(x), x)
new_df <- merge(df, x, by = "interval", all.x = T)
names(x) <- c("interval", "steps")
# Create a new dataset that is equal to the original dataset but with the missing data filled in.
new_df <- merge(df, x, by = "interval", all.x = T)
?plot
weekdays(new_df$date)
?weekdays
weekdays(new_df$date, abbreviate = T)
weekdays(new_df$date)
weekdays(new_df$date)=="星期六"
weekdays(new_df$date)=="星期六" | weekdays(new_df$date)=="星期日"
table(weekdays(new_df$date)=="星期六" | weekdays(new_df$date)=="星期日")
table(weekdays(new_df$date)=="星期六")
new_df <- cbind(df, weekdays(new_df$date)=="星期六" | weekdays(new_df$date)=="星期日"))
new_df <- cbind(df, weekdays(new_df$date)=="星期六" | weekdays(new_df$date)=="星期日")
View(new_df)
View(new_df)
View(new_df)
head(weekdays(new_df$date)=="星期六" | weekdays(new_df$date)=="星期日")
new_df <- cbind(new_df, (weekdays(new_df$date)=="星期六" | weekdays(new_df$date)=="星期日"))
df <- read.csv("activity.csv")
# Process/transform the data (if necessary) into a format suitable for your analysis
df$date <- as.Date(df$date)
```
## What is mean total number of steps taken per day?
```{r}
# Calculate the total number of steps taken per day
total_steps_per_day <- tapply(df$steps, df$date, sum, na.rm = TRUE)
# Make a histogram of the total number of steps taken each day
hist(total_steps_per_day)
# Calculate and report the mean and median of the total number of steps taken per day
mean(total_steps_per_day)
median(total_steps_per_day)
```
## What is the average daily activity pattern?
```{r}
# Make a time series plot (i.e. type = "l") of the 5-minute interval (x-axis) and the average number of steps taken, averaged across all days (y-axis)
average_steps_by_interval <- tapply(df$steps, df$interval, mean, na.rm=T)
plot(names(average_steps_by_interval), average_steps_by_interval, type="l")
# Which 5-minute interval, on average across all the days in the dataset, contains the maximum number of steps?
names(which.max(average_steps_by_interval))
```
## Imputing missing values
```{r}
# Calculate and report the total number of missing values in the dataset
# Devise a strategy for filling in all of the missing values in the dataset. The strategy does not need to be sophisticated. For example, you could use the mean/median for that day, or the mean for that 5-minute interval, etc.
nrow(df[is.na(df$steps),])
x <- as.data.frame(average_steps_by_interval)
x <- cbind(row.names(x), x)
names(x) <- c("interval", "steps")
# Create a new dataset that is equal to the original dataset but with the missing data filled in.
new_df <- merge(df, x, by = "interval", all.x = T)
new_df[is.na(new_df$steps.x),"steps.x"] <- new_df[is.na(new_df$steps.x),"steps.y"]
# Make a histogram of the total number of steps taken each day and Calculate and report the mean and median total number of steps taken per day. Do these values differ from the estimates from the first part of the assignment? What is the impact of imputing missing data on the estimates of the total daily number of steps?
new_total_steps_per_day <- tapply(new_df$steps.x, new_df$date, sum, na.rm = TRUE)
hist(new_total_steps_per_day)
mean(new_total_steps_per_day)
median(new_total_steps_per_day)
```
## Are there differences in activity patterns between weekdays and weekends?
```{r}
# Create a new factor variable in the dataset with two levels – “weekday” and “weekend” indicating whether a given date is a weekday or weekend day.
new_df <- cbind(new_df, (weekdays(new_df$date)=="星期六" | weekdays(new_df$date)=="星期日"))
# Make a panel plot containing a time series plot (i.e. type = "l") of the 5-minute interval (x-axis) and the average number of steps taken, averaged across all weekday days or weekend days (y-axis). See the README file in the GitHub repository to see an example of what this plot should look like using simulated data.
```
View(new_df)
?cbind
names(new_df)
names(new_df[5])
names(new_df[5]) <- "Weekend"
names(new_df[5])
class(new_df[5])
class(new_df[3])
class(new_df[4])
class(new_df[2])
class(new_df[2,])
class(new_df[4,])
class(new_df[3,])
class(new_df[5,])
View(new_df)
View(new_df)
View(new_df)
```{r}
# Load the data
df <- read.csv("activity.csv")
# Process/transform the data (if necessary) into a format suitable for your analysis
df$date <- as.Date(df$date)
```
## What is mean total number of steps taken per day?
```{r}
# Calculate the total number of steps taken per day
total_steps_per_day <- tapply(df$steps, df$date, sum, na.rm = TRUE)
# Make a histogram of the total number of steps taken each day
hist(total_steps_per_day)
# Calculate and report the mean and median of the total number of steps taken per day
mean(total_steps_per_day)
median(total_steps_per_day)
```
## What is the average daily activity pattern?
```{r}
# Make a time series plot (i.e. type = "l") of the 5-minute interval (x-axis) and the average number of steps taken, averaged across all days (y-axis)
average_steps_by_interval <- tapply(df$steps, df$interval, mean, na.rm=T)
plot(names(average_steps_by_interval), average_steps_by_interval, type="l")
# Which 5-minute interval, on average across all the days in the dataset, contains the maximum number of steps?
names(which.max(average_steps_by_interval))
```
## Imputing missing values
```{r}
# Calculate and report the total number of missing values in the dataset
nrow(df[is.na(df$steps),])
# Devise a strategy for filling in all of the missing values in the dataset. The strategy does not need to be sophisticated. For example, you could use the mean/median for that day, or the mean for that 5-minute interval, etc.
x <- as.data.frame(average_steps_by_interval)
x <- cbind(row.names(x), x)
names(x) <- c("interval", "steps")
# Create a new dataset that is equal to the original dataset but with the missing data filled in.
new_df <- merge(df, x, by = "interval", all.x = T)
new_df[is.na(new_df$steps.x),"steps.x"] <- new_df[is.na(new_df$steps.x),"steps.y"]
# Make a histogram of the total number of steps taken each day and Calculate and report the mean and median total number of steps taken per day. Do these values differ from the estimates from the first part of the assignment? What is the impact of imputing missing data on the estimates of the total daily number of steps?
new_total_steps_per_day <- tapply(new_df$steps.x, new_df$date, sum, na.rm = TRUE)
hist(new_total_steps_per_day)
mean(new_total_steps_per_day)
median(new_total_steps_per_day)
```
## Are there differences in activity patterns between weekdays and weekends?
```{r}
# Create a new factor variable in the dataset with two levels – “weekday” and “weekend” indicating whether a given date is a weekday or weekend day.
new_df <- cbind(new_df, (weekdays(new_df$date)=="星期六" | weekdays(new_df$date)=="星期日"))
new_df[5,]
new_df[,5]
class(new_df[,5])
levels(new_df[,5])
as.factor(new_df[,5])
levels(new_df[,5])
level(new_df[,5])
```{r}
# Load the data
df <- read.csv("activity.csv")
# Process/transform the data (if necessary) into a format suitable for your analysis
df$date <- as.Date(df$date)
```
## What is mean total number of steps taken per day?
```{r}
# Calculate the total number of steps taken per day
total_steps_per_day <- tapply(df$steps, df$date, sum, na.rm = TRUE)
# Make a histogram of the total number of steps taken each day
hist(total_steps_per_day)
# Calculate and report the mean and median of the total number of steps taken per day
mean(total_steps_per_day)
median(total_steps_per_day)
```
## What is the average daily activity pattern?
```{r}
# Make a time series plot (i.e. type = "l") of the 5-minute interval (x-axis) and the average number of steps taken, averaged across all days (y-axis)
average_steps_by_interval <- tapply(df$steps, df$interval, mean, na.rm=T)
plot(names(average_steps_by_interval), average_steps_by_interval, type="l")
# Which 5-minute interval, on average across all the days in the dataset, contains the maximum number of steps?
names(which.max(average_steps_by_interval))
```
## Imputing missing values
```{r}
# Calculate and report the total number of missing values in the dataset
nrow(df[is.na(df$steps),])
# Devise a strategy for filling in all of the missing values in the dataset. The strategy does not need to be sophisticated. For example, you could use the mean/median for that day, or the mean for that 5-minute interval, etc.
x <- as.data.frame(average_steps_by_interval)
x$interval <- row.names(x)
x
names(x) <- c("interval", "steps")
= "interval", all.x = T)
new_df[is.na(new_df$steps.x),"steps.x"] <- new_df[is.na(new_df$steps.x),"steps.y"]
# Make a histogram of the total number of steps taken each day and Calculate and report the mean and median total number of steps taken per day. Do these values differ from the estimates from the first part of the assignment? What is the impact of imputing missing data on the estimates of the total daily number of steps?
new_total_steps_per_day <- tapply(new_df$steps.x, new_df$date, sum, na.rm = TRUE)
hist(new_total_steps_per_day)
mean(new_total_steps_per_day)
median(new_total_steps_per_day)
new_total_steps_per_day <- tapply(new_df$steps.x, new_df$date, sum, na.rm = TRUE)
new_df <- merge(df, x, by = "interval", all.x = T)
new_df[is.na(new_df$steps.x),"steps.x"] <- new_df[is.na(new_df$steps.x),"steps.y"]
new_total_steps_per_day <- tapply(new_df$steps.x, new_df$date, sum, na.rm = TRUE)
hist(new_total_steps_per_day)
mean(new_total_steps_per_day)
median(new_total_steps_per_day)
new_total_steps_per_day <- tapply(new_df$steps.x, new_df$date, sum, na.rm = TRUE)
# Load the data
df <- read.csv("activity.csv")
# Process/transform the data (if necessary) into a format suitable for your analysis
df$date <- as.Date(df$date)
```
## What is mean total number of steps taken per day?
```{r}
# Calculate the total number of steps taken per day
total_steps_per_day <- tapply(df$steps, df$date, sum, na.rm = TRUE)
# Make a histogram of the total number of steps taken each day
hist(total_steps_per_day)
# Calculate and report the mean and median of the total number of steps taken per day
mean(total_steps_per_day)
median(total_steps_per_day)
```
## What is the average daily activity pattern?
```{r}
# Make a time series plot (i.e. type = "l") of the 5-minute interval (x-axis) and the average number of steps taken, averaged across all days (y-axis)
average_steps_by_interval <- tapply(df$steps, df$interval, mean, na.rm=T)
plot(names(average_steps_by_interval), average_steps_by_interval, type="l")
# Which 5-minute interval, on average across all the days in the dataset, contains the maximum number of steps?
names(which.max(average_steps_by_interval))
```
## Imputing missing values
```{r}
# Calculate and report the total number of missing values in the dataset
nrow(df[is.na(df$steps),])
# Devise a strategy for filling in all of the missing values in the dataset. The strategy does not need to be sophisticated. For example, you could use the mean/median for that day, or the mean for that 5-minute interval, etc.
x <- as.data.frame(average_steps_by_interval)
x <- cbind(row.names(x), x)
names(x) <- c("interval", "steps")
# Create a new dataset that is equal to the original dataset but with the missing data filled in.
new_df <- merge(df, x, by = "interval", all.x = T)
new_df[is.na(new_df$steps.x),"steps.x"] <- new_df[is.na(new_df$steps.x),"steps.y"]
# Make a histogram of the total number of steps taken each day and Calculate and report the mean and median total number of steps taken per day. Do these values differ from the estimates from the first part of the assignment? What is the impact of imputing missing data on the estimates of the total daily number of steps?
new_total_steps_per_day <- tapply(new_df$steps.x, new_df$date, sum, na.rm = TRUE)
hist(new_total_steps_per_day)
mean(new_total_steps_per_day)
median(new_total_steps_per_day)
?factor
f <- factor("abc", "xyz", "abc")
f
f <- factor(C("abc", "xyz", "abc"))
f <- factor(C("abc", "xyz"))
f <- as.factor(C("abc", "xyz", "abc"))
f <- as.factor("abc", "xyz", "abc")
f <- as.factor("abc", "xyz")
f
f <- as.factor("abc", "xyz")
f <- factor("abc", "xyz")
f
f <- factor("abc", "xyz")
f
f <- factor("abc", "xyz", "abc")
f
f <- factor("abc", "xyz", "kkk")
f
f[1]
f[2]
levels(f)
f <- factor(x = c(1,2,1,2,1), levels = c("abc", "xyz"))
f
f <- c("abc", "xyz", "abc")
f
f <- as.factor(f)
f
levels(f)
levels(f) <- c("aaa", "bbb")
f
new_df$weekdays <- as.factor(weekdays(new_df$date)=="星期六" | weekdays(new_df$date)=="星期日")
View(x)
View(new_df)
levels(new_df$weekdays)
levels(new_df$weekdays) <- c("weekday", "weekend")
View(new_df)
par(mfrow= c(2,1))
weekday_average_steps <- tapply(new_df[new_df$weekdays=="weekday"]$steps, new_df[new_df$weekdays=="weekday"]$interval, mean, na.rm=T)
View(new_df)
new_df$weekdays=="weekday"
new_df[new_df$weekdays=="weekday","steps"]
new_df[new_df$weekdays=="weekday",]
new_df[new_df$weekdays=="weekday",]$steps
new_df[new_df$weekdays=="weekend",]
new_df[new_df$weekdays=="weekend"]
new_df[new_df$weekdays=="weekend",]
new_df[new_df$weekdays=="weekend",2]
new_df[new_df$weekdays=="weekend","steps.x"]
new_df[new_df$weekdays=="weekend",]$steps.x
weekday_average_steps <- tapply(new_df[new_df$weekdays=="weekday","steps.x"], new_df[new_df$weekdays=="weekday", "interval"], mean, na.rm=T)
weekday_average_steps
weekday_average_steps <- tapply(new_df[new_df$weekdays=="weekday","steps.x"], new_df[new_df$weekdays=="weekday", "interval"], mean, na.rm=T)
par(mfrow= c(2,1))
weekday_average_steps <- tapply(new_df[new_df$weekdays=="weekday","steps.x"], new_df[new_df$weekdays=="weekday", "interval"], mean, na.rm=T)
plot(names(weekday_average_steps), weekday_average_steps, type="l")
weekend_average_steps <- tapply(new_df[new_df$weekdays=="weekend","steps.x"], new_df[new_df$weekdays=="weekend", "interval"], mean, na.rm=T)
plot(names(weekend_average_steps), weekend_average_steps, type="l")
par(mfrow= c(2,1))
weekday_average_steps <- tapply(new_df[new_df$weekdays=="weekday","steps.x"], new_df[new_df$weekdays=="weekday", "interval"], mean, na.rm=T)
plot(names(weekday_average_steps), weekday_average_steps, type="l", ylab = "weekday")
weekend_average_steps <- tapply(new_df[new_df$weekdays=="weekend","steps.x"], new_df[new_df$weekdays=="weekend", "interval"], mean, na.rm=T)
plot(names(weekend_average_steps), weekend_average_steps, type="l", ylab = "weekend")
par(mfrow= c(2,1))
weekday_average_steps <- tapply(new_df[new_df$weekdays=="weekday","steps.x"], new_df[new_df$weekdays=="weekday", "interval"], mean, na.rm=T)
plot(names(weekday_average_steps), weekday_average_steps, type="l", xlab = "", ylab = "weekday")
weekend_average_steps <- tapply(new_df[new_df$weekdays=="weekend","steps.x"], new_df[new_df$weekdays=="weekend", "interval"], mean, na.rm=T)
plot(names(weekend_average_steps), weekend_average_steps, type="l", ylab = "weekend")
par(mfrow= c(1,2))
weekday_average_steps <- tapply(new_df[new_df$weekdays=="weekday","steps.x"], new_df[new_df$weekdays=="weekday", "interval"], mean, na.rm=T)
plot(names(weekday_average_steps), weekday_average_steps, type="l", xlab = "", ylab = "weekday")
weekend_average_steps <- tapply(new_df[new_df$weekdays=="weekend","steps.x"], new_df[new_df$weekdays=="weekend", "interval"], mean, na.rm=T)
plot(names(weekend_average_steps), weekend_average_steps, type="l", ylab = "weekend")
